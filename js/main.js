'use strict';

function ten(str) {
    for (let i=0; i<10; i++)
    console.log(str);
    // console.log(`${i}patata`)
}

ten('Patata');
ten('Aguacate');
ten('Pizza');
// esto llama a la función, si no se llama no funciona.